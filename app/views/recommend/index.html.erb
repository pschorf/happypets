<h1>Recommendations</h1>
<ul>
  <% for pet in @matches.keys %>
    <% if @matches[pet] %>
      <li>We recommend <a href="<%= pet_path(@matches[pet].id) %>"><%= @matches[pet].name %></a> for <a href="<%= pet_path(pet.id) %>"><%= pet.name %></a>.</li>
    <% else %>
      <li>We couldn't find a match for <a href="<%= pet_path(pet.id) %>"><%= pet.name %></a>.</li>
    <% end %>
  <% end %>
</ul>